TYPE "UDT_Silos" :
STRUCT
    // ===== Дискретні входи (HAL / залізо) =====
    DI_LevelHigh    : BOOL;    // рівень — верхній
    DI_LevelLow     : BOOL;    // рівень — нижній

    // ===== Аналогові входи =====
    AI_Level        : INT;     // аналоговий рівень (raw)
END_STRUCT;
END_TYPE
